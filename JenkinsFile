pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git url: 'https://github.com/ducime/projet-M1-RES', branch: 'main'
            }
        }
    }
}
