pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git url: 'https://github.com/utilisateur/mon-repo.git', branch: 'main'
            }
        }
    }
}
